# MIE498_OpenFOAM

To run use:
- Move desired mesh from `Meshes` folder into main directory 
- Run `fluent3DMeshToFoam <meshName.msh>`
- Use solver of choice. Project is configured for turbulence modelling so `pisoFoam` is probably best. 